*** Variables ***
${MEDA_END_POINT}    http://test.fepblue.org/meda/getlasthit.json

*** Keywords ***
Page Hit Should Have Been Recorded In MEDA
    [Arguments]    ${page_hit}
    Create Session    meda_endpoint    ${MEDA_END_POINT}
    ${data}=    Create Dictionary    dataset    b1f6a62966cc4fc38ffc0897622567d7
    ${headers}=    Create Dictionary    Content-Type    application/json
    ${resp}=    Post    meda_endpoint    /post    body=${data}    headers=${headers}
    #Should Contain    ${resp.status_code}    /web/guest/myblue
    Should Be Equal    ${resp.status_code}    200
